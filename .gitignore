# Claude Code local session/tooling state — not part of the product
.claude/

# Unrelated project sharing this folder (separate PDF-generator tool)
lesson-pdf-generator/
course pdfs/

# OS junk
.DS_Store
Thumbs.db

# Test dependencies — installed via `npm install` inside tests/
tests/node_modules/
tests/package-lock.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
